// Solution: Improved error handling and more informative logging

expo.build:android --public-url=https://example.com --release-channel=production

// Check build logs for more detail. This is where the real debugging happens!

// Clean and rebuild the android project

expo prebuild -c
expo build:android